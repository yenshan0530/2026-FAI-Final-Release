## 2026-04-08 - Patched FullCFRPlayer Eval

- Target: Patched `FullCFRPlayer` in `src/players/cfr/full_cfr_player.py`.
- Setup: Official `RandomPartitionTournamentRunner`; 4 players; 10 rounds; `timeout=1.0`; `duplication_mode="cycle"`; 200 partitions; 800 CFR games total; opponents were 3 `RandomPlayer`s. Existing regression test `tests/test_full_cfr_player.py` passed before the benchmark.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, first/top2/last rates, `TO/EXC/DQ/ERR`, CFR-path activation frequency.
- Comparison to baseline: Old baseline was `avg_rank=1.9406`, `avg_score=8.1813`, `est_elo=1602.13`, `TO=0`, `EXC=0`, `DQ=0`, `ERR=0`. New result was `avg_rank=1.8106`, `avg_score=7.5325`, `est_elo=1629.47`, `TO=0`, `EXC=0`, `DQ=0`, `ERR=0`. Delta: `avg_rank -0.1300`, `avg_score -0.6488`, `est_elo +27.34`.
- Outcome: Improved.
- Representative failures: No new correctness failures were observed. The main remaining risk is runtime cost: the official benchmark took about `1732.1s` wall-clock. Last-place finishes still occurred in `7.12%` of games.
- Interpretation: The guard fix restored real CFR execution during tournament play and the patched player outperformed the prior heuristic-only baseline on this benchmark. CFR was used on `7200/7200` nontrivial decisions (`100%` activation; `7200/8000` of all actions including trivial final-card turns).
- Recommendation: Improve. Keep the reachability fix, then reduce solver cost and validate against stronger baselines than `RandomPlayer`.
- Status: Complete. Benchmark rerun finished on the official runner with subprocess-safe activation logging.

## 2026-04-08 - CFR row-helper refactor microbenchmark

- Target: `src/players/cfr/full_cfr_player.py`
- Change: Replaced duplicated board-row selection logic with shared `_best_row_for_card()` / `_lowest_penalty_row()` helpers and cached per-card bullhead values in `self._card_scores`.
- Equivalence check: Loaded the pre-change player from `/tmp/full_cfr_player_before.py` and compared `_place_card()`, `_card_risk_snapshot()`, `_heuristic_scores()`, and `_profile_utilities()` on `250` randomized board/hand cases. Result: exact match on all `250/250` cases.
- Microbenchmark: On the same `250` prepared cases, ran `_heuristic_scores()` plus `_profile_utilities()` for `120` loops, with `3` timing repeats per version. Before: `1.948180s, 1.849114s, 1.926280s`; after: `1.632820s, 1.677210s, 1.732072s`.
- Outcome: Improved.
- Best-case delta: `1.849114s -> 1.632820s` (`11.70%` faster).
- Status: Complete.

## 2026-04-08 - Official rerun after CFR helper refactor

- Target: Current `FullCFRPlayer` in `src/players/cfr/full_cfr_player.py`.
- Setup: Official `RandomPartitionTournamentRunner`; 4 players; 10 rounds; `timeout=1.0`; `duplication_mode="cycle"`; 200 partitions; 800 CFR games total; opponents were 3 `RandomPlayer`s.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `TO/EXC/DQ/ERR`.
- Comparison to baseline: This rerun produced `avg_rank=1.8919`, `avg_score=7.7575`, `est_elo=1612.08`, `TO=0`, `EXC=0`, `DQ=0`, `ERR=0`. Versus the prior logged official rerun (`1.8106 / 7.5325 / 1629.47`), this is worse by `+0.0813 avg_rank`, `+0.2250 avg_score`, and `-17.39 est_elo`. Versus the older heuristic-only baseline (`1.9406 / 8.1813 / 1602.13`), it is still better by `-0.0487 avg_rank`, `-0.4238 avg_score`, and `+9.95 est_elo`.
- Outcome: Inconclusive for overall progress; still above the old heuristic baseline, but below the most recent official rerun.
- Representative failures: No new correctness failures were observed and `TO/EXC/DQ/ERR` remained zero. The requested public-baseline comparison could not be executed locally because `src/players/TA/public_baselines1.cpython-313-x86_64-linux-gnu.so` fails to load under local `Python 3.10.12` with `undefined symbol: _PyIntrinsics_UnaryFunctions`, and no `python3.13` interpreter is installed on this machine.
- Interpretation: Current score/rank remains solid on the documented random-baseline setup, but the latest rerun shows measurable variance relative to the previous official run. The local environment cannot answer the stronger public-baseline question yet.
- Recommendation: Eval in a Python 3.13 environment that can import the TA public baselines, then compare against those baselines directly. Do not treat local random-baseline-only results as a substitute for that comparison.
- Status: Complete.

## 2026-04-08 - AlphaZeroLitePlayer eval vs RandomPlayer baseline

- Target: `src/players/alpha_zero_lite` via `AlphaZeroLitePlayer` from `src.players.alpha_zero_lite`.
- Setup: README-style `random_partition` tournament using `python run_tournament.py --config configs/tournament/example.json --player-cfg /tmp/azl_players_random.json --tournament-cfg /tmp/azl_tournament_cycle_200.json`. Engine settings came from `configs/tournament/example.json` (`n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`); tournament override used `duplication_mode="cycle"`, `num_games_per_player=200`, `num_workers=4`. Lineup was `AlphaZeroLitePlayer + 3 RandomPlayer`s. Result file: `results/tournament/2026-04-08_16-54-08_example.json`.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `games_played`, `TO/EXC/DQ/ERR`, partition first/top2/last counts, wall-clock time.
- Raw results: `AlphaZeroLitePlayer` finished with `avg_rank=1.8900`, `avg_score=7.6500`, `est_elo=1612.48`, `games=800`, `TO/EXC/DQ/ERR=0/0/0/0`, wall-clock `21.32s`. Random baselines were `R1: 2.6819 / 13.6238 / 1466.40`, `R2: 2.7363 / 14.1613 / 1456.53`, `R3: 2.6919 / 13.7338 / 1464.59`. Partition summary for `AlphaZeroLitePlayer`: `1st=130/200`, `top2=171/200`, `last=4/200`.
- Comparison to baseline: Versus the mean of the three `RandomPlayer` baselines, `AlphaZeroLitePlayer` improved by `-0.8133 avg_rank`, `-6.1896 avg_score`, and `+149.98 est_elo`. Versus the strongest random baseline (`R1`), the deltas were `-0.7919 avg_rank`, `-5.9738 avg_score`, and `+146.09 est_elo`. A stronger comparison was attempted with `/tmp/azl_vs_public_baselines.json`, but `python run_tournament.py --config /tmp/azl_vs_public_baselines.json` exited with `ImportError` because `src.players.TA.public_baselines1` is only available for `CPython 3.13` while the local interpreter is `CPython 3.10.12`.
- Outcome: Improved against the random-player baseline; public-baseline comparison remains inconclusive locally.
- Representative failures: Partition `192` was the clearest bad table: `AlphaZeroLitePlayer` averaged `rank=3.00`, `score=18.00`, while the three random baselines posted `1.875/8.75`, `2.500/12.25`, and `2.625/13.25`. Partition `59` also ended in 4th with `avg_rank=3.00`, `avg_score=16.25`. Partition `52` was a useful non-last failure mode: `AlphaZeroLitePlayer` still placed 2nd, but its average score spiked to `17.50`, showing bullhead-heavy outcomes can remain possible even in nominally acceptable finishes.
- Interpretation: On the available local benchmark, `AlphaZeroLitePlayer` is clearly stronger than `RandomPlayer` and ran comfortably within the time budget, with no tournament failure markers. The main remaining uncertainty is not basic correctness or random-baseline strength, but performance against the TA public baselines or other stronger opponents.
- Recommendation: Improve. Keep the current implementation as a solid random-baseline-beating draft, then run the next evaluation under `Python 3.13` against the TA public baselines before making claims about final tournament competitiveness.
- Status: Complete for the local random-baseline benchmark; stronger-baseline comparison blocked by interpreter mismatch.

## 2026-04-08 - AlphaZeroLitePlayer attempt vs TA public baselines on Python 3.13

- Target: `src/players/alpha_zero_lite` via `AlphaZeroLitePlayer` from `src.players.alpha_zero_lite`.
- Setup: The user-provided `[SETUP]` placeholder was not expanded, so I used the closest existing public-baseline setup from `configs/tournament/example_2.json`: 4 players, 10 rounds, `timeout=1.0`, `timeout_buffer=0.5`, `random_partition`, `duplication_mode="cycle"`, public baselines `B1`-`B5`, and a player lineup of `AlphaZeroLitePlayer`, `Baseline4`, `Baseline5`, and `RandomPlayer`. To isolate baseline loading under the now-available `/usr/bin/python3.13` (`Python 3.13.12`), I ran a 20-partition smoke config at `num_workers=1` from `/tmp/azl_vs_public_baselines_smoke.json`. Because this interpreter lacked `tqdm` and `joblib`, temporary `/tmp/py313_shims` modules were supplied via `PYTHONPATH` only so the tournament runner could start without changing repo code. Separately, `tests/test_alpha_zero_lite_player.py` passed under `python3.13` when invoked via `runpy`.
- Metrics: No gameplay metrics were produced because the run aborted before the first game. The closest measurable outputs were `exit-by-signal=11` (`SIGSEGV`) and wall-clock `0.13s` on the smoke tournament command.
- Comparison to baseline: Not available. The TA baseline binary crashed during import, so no score/rank comparison could be measured.
- Outcome: Inconclusive and blocked.
- Representative failures: Direct import `python3.13 -X faulthandler -c 'import importlib; importlib.import_module("src.players.TA.public_baselines1")'` terminated with a segmentation fault inside importlib extension loading. The example_2-derived tournament path also terminated with `SIGSEGV` during `load_players()` before any matchup started.
- Interpretation: This is not evidence of a new `AlphaZeroLitePlayer` gameplay regression. `AlphaZeroLitePlayer` itself still passes its targeted late-game regression test under `python3.13`. The blocking issue is that `src.players.TA.public_baselines1` is currently not usable in this local `python3.13.12` environment because importing the TA baseline extension crashes the interpreter.
- Recommendation: Debug. Fix or replace the local TA public-baseline runtime environment first, then rerun the public-baseline comparison; do not treat the environment as baseline-ready just because `python3.13` is installed.
- Status: Blocked before gameplay; no benchmark comparison recorded.

## 2026-04-08 - FullCFRPlayer attempt vs TA public baseline on Python 3.13

- Target: Current `FullCFRPlayer` in `src/players/cfr/full_cfr_player.py`, with intended comparison against the shipped TA public baseline artifact `src/players/TA/public_baselines1.cpython-313-x86_64-linux-gnu.so`.
- Setup: Re-read `CODEXAGENT.MD`, `docs/current_findings.md`, and `docs/eval_log.md`; confirmed `/usr/bin/python3.13` is available as `Python 3.13.12`; created clean venv `/tmp/cfr-eval-py313`; installed `numpy`, `joblib`, and `tqdm` into that venv; reran the official CFR-only benchmark on the documented setup (`RandomPartitionTournamentRunner`, 4 players, 10 rounds, `timeout=1.0`, `duplication_mode="cycle"`, 200 partitions, 800 games) using the current default `python` to keep current score/rank data aligned.
- Metrics: Current CFR-only benchmark on that setup produced `avg_rank=1.8919`, `avg_score=7.7575`, `est_elo=1612.08`, `TO/EXC/DQ/ERR=0/0/0/0`.
- Comparison to baseline: No TA-baseline score/rank comparison was produced. Importing `src.players.TA.public_baselines1` segfaulted under `/usr/bin/python3.13` and also segfaulted under `/tmp/cfr-eval-py313/bin/python` after installing `numpy`, `joblib`, and `tqdm`, so no local head-to-head CFR-vs-TA-baseline benchmark could start.
- Outcome: Inconclusive and blocked.
- Representative failures: `python3.13 -X faulthandler -c "import src.players.TA.public_baselines1"` crashed with `Fatal Python error: Segmentation fault` during extension loading. The same crash reproduced in the clean venv after dependency installation.
- Interpretation: The earlier interpreter-mismatch blocker is no longer the only issue. Even with a working `python3.13` interpreter and the minimal runtime packages present, the shipped TA public baseline artifact is not stable enough to import locally.
- Recommendation: Debug the TA public baseline runtime before requesting more local CFR-vs-TA-baseline evals in this workspace. Until that import crash is fixed, TA-baseline comparison here remains unavailable.
- Status: Blocked before gameplay; no benchmark comparison recorded.

## 2026-04-09 - FullCFRPlayer conda rerun vs RandomPlayer and TA public baselines

- Target: `src/players/cfr/full_cfr_player.py` via `FullCFRPlayer`.
- Setup: User-provided conda env `FAI_final` (`/home/thoamslai/miniconda3/envs/FAI_final/bin/python`, `Python 3.13.11`) successfully imported `src.players.TA.public_baselines1`. Two 200-partition `random_partition` / `cycle` tournaments were evaluated with `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, and `num_workers=4`. The random-baseline lineup was `FullCFRPlayer + 3 RandomPlayer`s from `results/tournament/2026-04-09_23-29-44_example.json`. The public-baseline lineup was `FullCFRPlayer + Baseline4 + Baseline5 + RandomPlayer` with baseline anchors `B1`-`B5` from `results/tournament/2026-04-09_23-43-04_cfr_vs_public.json`.
- Metrics: Random-baseline run: `avg_rank=1.851875`, `avg_score=7.50125`, `est_elo=1620.54`, partition `first/top2/last=46/135/5`, `TO/EXC/DQ/ERR=0/0/0/0`. Public-baseline run: `avg_rank=2.12`, `avg_score=9.41875`, `est_elo=1582.89`, calibrated `Score=100.00`, partition `first/top2/last=16/93/16`, `TO/EXC/DQ/ERR=0/0/0/0`.
- Comparison to baseline: On the random-baseline setup, this rerun beat the older heuristic-only baseline `1.9406 / 8.1813 / 1602.13` by `-0.088725 avg_rank`, `-0.68005 avg_score`, and `+18.41 est_elo`, and also beat the later rerun `1.8919 / 7.7575 / 1612.08` by `-0.040025 avg_rank`, `-0.25625 avg_score`, and `+8.46 est_elo`. It remained behind the best logged patched rerun `1.8106 / 7.5325 / 1629.47` on `avg_rank` and `est_elo`, though its `avg_score` was slightly better. On the public-baseline setup, `FullCFRPlayer` beat the strongest baseline entrant (`Baseline4` as a player) at `2.246875 / 10.6575 / 1555.87` by `-0.126875 avg_rank`, `-1.23875 avg_score`, and `+27.02 est_elo`.
- Outcome: Improved.
- Representative failures: Random-baseline worst partitions were `53` (`avg_rank=3.25`, `avg_score=13.25`), `34` (`3.00`, `18.25`), and `74` (`3.00`, `13.25`). Public-baseline worst partitions were `153` (`3.50`, `20.75`), `29` (`3.375`, `14.00`), and `158` (`3.25`, `13.50`).
- Interpretation: In the conda environment that can load the TA baselines, `FullCFRPlayer` remained clearly above `RandomPlayer` and also posted a stronger score/rank line than the best TA baseline entry on the 200-partition public-baseline setup. The main remaining weakness is cost, not correctness.
- Recommendation: Improve. Keep `FullCFRPlayer` as a strong candidate, then reduce runtime and rerun at larger sample counts to confirm the public-baseline edge is stable.
- Status: Complete.

## 2026-04-09 - AlphaZeroLitePlayer conda rerun vs RandomPlayer and TA public baselines

- Target: `src/players/alpha_zero_lite` via `AlphaZeroLitePlayer`.
- Setup: User-provided conda env `FAI_final` (`/home/thoamslai/miniconda3/envs/FAI_final/bin/python`, `Python 3.13.11`) with working TA baseline imports. Two 200-partition `random_partition` / `cycle` tournaments were evaluated with `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, and `num_workers=4`. The random-baseline lineup was `AlphaZeroLitePlayer + 3 RandomPlayer`s from `results/tournament/2026-04-09_23-32-08_example.json`. The public-baseline lineup was `AlphaZeroLitePlayer + Baseline4 + Baseline5 + RandomPlayer` with baseline anchors `B1`-`B5` from `results/tournament/2026-04-09_23-33-05_azl_vs_public.json`.
- Metrics: Random-baseline run: `avg_rank=1.990625`, `avg_score=8.2825`, `est_elo=1592.19`, partition `first/top2/last=25/122/11`, `TO/EXC/DQ/ERR=0/0/0/0`. Public-baseline run: `avg_rank=2.173125`, `avg_score=9.59375`, `est_elo=1572.03`, calibrated `Score=100.00`, partition `first/top2/last=15/89/19`, `TO/EXC/DQ/ERR=0/0/0/0`.
- Comparison to baseline: The random-baseline rerun was worse than the prior logged AlphaZeroLite random benchmark `1.8900 / 7.6500 / 1612.48` by `+0.100625 avg_rank`, `+0.6325 avg_score`, and `-20.29 est_elo`. On the public-baseline setup, `AlphaZeroLitePlayer` still beat the strongest baseline entrant (`Baseline5` baseline anchor) at `2.20375 / 10.29375 / 1564.51` by `-0.030625 avg_rank`, `-0.70 avg_score`, and `+7.51 est_elo`.
- Outcome: Mixed.
- Representative failures: Random-baseline worst partitions were `160` (`avg_rank=3.50`, `avg_score=17.25`), `54` (`3.25`, `13.00`), and `57` (`3.125`, `19.00`). Public-baseline worst partitions were `147` (`3.625`, `26.00`), `44` (`3.50`, `17.00`), and `108` (`3.50`, `16.00`).
- Interpretation: `AlphaZeroLitePlayer` is still competitive enough to beat the strongest TA baseline entry on this 200-partition public-baseline setup, but its fresh random-baseline rerun was materially weaker than the earlier logged random-baseline result. That looks like underperformance rather than a correctness break because failure markers stayed at zero and the player still remained first in both standalone runs.
- Recommendation: Improve. Keep the approach, but investigate the variance or configuration sensitivity behind the weaker random-baseline rerun before declaring it stable.
- Status: Complete.

## 2026-04-10 - Joint FullCFRPlayer and AlphaZeroLitePlayer tournament vs TA public baselines

- Target: Direct side-by-side evaluation of `FullCFRPlayer` and `AlphaZeroLitePlayer`.
- Setup: Executed locally with `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` (`Python 3.13.11`) using `/tmp/cfr_azl_vs_public.json`. Lineup: `FullCFRPlayer`, `AlphaZeroLitePlayer`, `Baseline4`, `Baseline5`, and `RandomPlayer`, plus baseline anchors `B1`-`B5`. Tournament settings were `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=200`, `num_workers=4`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`. The 10-player pool was padded with 2 `RandomPlayer`s to 12 entrants. Result file: `results/tournament/2026-04-10_00-03-54_cfr_azl_vs_public.json`.
- Metrics: `AlphaZeroLitePlayer`: `avg_rank=2.225625`, `avg_score=10.46875`, `est_elo=1558.60`, calibrated `Score=100.00`, partition `first/top2/last=10/83/19`, `TO/EXC/DQ/ERR=0/0/0/0`. `FullCFRPlayer`: `avg_rank=2.230625`, `avg_score=10.31375`, `est_elo=1561.54`, calibrated `Score=100.00`, partition `first/top2/last=7/79/14`, `TO/EXC/DQ/ERR=0/0/0/0`.
- Comparison to baseline: Both agents finished ahead of the strongest baseline entry (`Baseline4` baseline anchor at `2.329375 / 11.35125 / 1539.41`). `AlphaZeroLitePlayer` beat that baseline by `-0.10375 avg_rank`, `-0.8825 avg_score`, and `+19.19 est_elo`; `FullCFRPlayer` beat it by `-0.09875 avg_rank`, `-1.0375 avg_score`, and `+22.12 est_elo`. Head-to-head, `AlphaZeroLitePlayer` had the slightly better overall `avg_rank` by `0.005`, while `FullCFRPlayer` had the lower `avg_score` by `0.155` and the higher `est_elo` by `2.94`. The two agents shared `53` partitions directly; across those, `AlphaZeroLitePlayer` outranked `FullCFRPlayer` in `29`, `FullCFRPlayer` outranked `AlphaZeroLitePlayer` in `19`, and `5` were ties.
- Outcome: Improved for both against the public-baseline field.
- Representative failures: `FullCFRPlayer` worst partitions were `24` (`avg_rank=3.50`, `avg_score=21.25`), `47` (`3.50`, `18.50`), and `74` (`3.50`, `16.50`). `AlphaZeroLitePlayer` worst partitions were `33` (`avg_rank=3.50`, `avg_score=22.50`), `192` (`3.50`, `21.75`), and `82` (`3.50`, `17.50`).
- Interpretation: Both agents are currently viable top-tier local candidates in the conda environment, and the direct comparison is close rather than decisive. `AlphaZeroLitePlayer` edges `FullCFRPlayer` on overall rank in this shared field, while `FullCFRPlayer` retains a small edge on raw score and Elo.
- Recommendation: Improve. Keep both players in the candidate set, then run a larger public-baseline tournament or a focused CFR-vs-AZL head-to-head to separate variance from a real ranking difference.
- Status: Complete.

## 2026-04-10 - Five rule-based modes vs TA public baselines

- Target: The five new rule-based modes under `src/players/`: `LowFirstSafeShedPlayer`, `ClosestFitConservativePlayer`, `HighFirstBlockerPlayer`, `ControlledResetSacrificePlayer`, and `LeaderPunishRankAwarePlayer`.
- Setup: Read `CODEXAGENT.MD`, `docs/current_findings.md`, and `docs/eval_log.md` first. The prompt referenced `[SETUP]` without expansion, so I reused the same public-baseline setup as the recent CFR/AZL evals in the working conda env `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` (`Python 3.13.11`). For each rule-based player, I ran one 200-partition `random_partition` / `cycle` tournament with `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, and `num_workers=4`. Each lineup was `candidate + Baseline4 + Baseline5 + RandomPlayer`, with baseline anchors `B1`-`B5`. Result files: `results/tournament/2026-04-10_01-17-30_rule_low_first_safe_shed_vs_public.json`, `results/tournament/2026-04-10_01-18-07_rule_closest_fit_conservative_vs_public.json`, `results/tournament/2026-04-10_01-19-05_rule_high_first_blocker_vs_public.json`, `results/tournament/2026-04-10_01-19-55_rule_controlled_reset_sacrifice_vs_public.json`, and `results/tournament/2026-04-10_01-20-37_rule_leader_punish_rank_aware_vs_public.json`.
- Metrics: `LFS`: `avg_rank=2.27875`, `avg_score=10.82625`, `est_elo=1551.86`, `Score=65.13`, partition `first/top2/last=8/79/27`, `TO/EXC/DQ/ERR=0/0/0/0`. `CFC`: `2.389375 / 11.55125 / 1519.44 / 0.00`, partition `6/61/37`, `TO/EXC/DQ/ERR=0/0/0/0`. `HFB`: `2.221875 / 9.96875 / 1559.57 / 100.00`, partition `17/83/22`, `TO/EXC/DQ/ERR=0/0/0/0`. `CRS`: `2.36125 / 11.47625 / 1532.80 / 5.25`, partition `6/65/31`, `TO/EXC/DQ/ERR=0/0/0/0`. `LPRA`: `2.261875 / 10.58 / 1556.76 / 95.43`, partition `10/79/26`, `TO/EXC/DQ/ERR=0/0/0/0`.
- Comparison to baseline: None of the five rule-based modes beat the strongest baseline entry by `avg_rank` in their standalone 200-partition runs. Deltas vs the strongest baseline in each run were: `LFS +0.0700 avg_rank, +0.58125 avg_score, -16.21 est_elo`; `CFC +0.1550, +1.5075, -43.68`; `HFB +0.00875, -0.0750, -6.82`; `CRS +0.105625, +1.22375, -21.13`; `LPRA +0.009375, -0.19375, +0.23`. By `avg_rank`, the strongest rule-based mode was `HFB`, followed closely by `LPRA`; by `avg_score`, `HFB` and `LPRA` were also the only rule-based modes that clearly matched top-baseline territory.
- Outcome: Mixed.
- Representative failures: `LFS` worst partitions were `153` (`avg_rank=3.50`, `avg_score=21.25`), `172` (`3.50`, `17.25`), and `179` (`3.50`, `17.25`). `CFC` worst partitions were `15` (`3.625`, `16.50`), `113` (`3.50`, `23.25`), and `36` (`3.50`, `21.50`). `HFB` worst partitions were `170` (`3.75`, `18.75`), `27` (`3.50`, `19.25`), and `41` (`3.50`, `18.00`). `CRS` worst partitions were `119` (`3.50`, `25.00`), `120` (`3.50`, `21.50`), and `63` (`3.50`, `19.00`). `LPRA` worst partitions were `125` (`4.00`, `23.00`), `154` (`3.625`, `24.50`), and `196` (`3.50`, `22.50`).
- Interpretation: The rule-based family is valid and robust, but not uniformly competitive. `HFB` and `LPRA` are credible lightweight opponents or teacher policies; `LFS`, `CFC`, and `CRS` remain clearly weaker against the TA baselines. The calibrated `Score` column is less informative than `avg_rank` here because clipping pushed `HFB` to `100.00` despite the strongest baseline still having a slightly better `avg_rank`.
- Recommendation: Improve. Keep `HFB` and `LPRA` as the promising rule-based candidates, and treat the other three as diversity/opponent policies unless they are refined further.
- Status: Complete.

## 2026-04-10 - All custom models tournament vs TA public baselines

- Target: Shared-field tournament for all current custom players: the five rule-based modes, `AlphaZeroLitePlayer`, and `FullCFRPlayer`.
- Setup: Executed with `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` (`Python 3.13.11`) using `/tmp/all_models_vs_public.json`. Lineup: `LFS`, `CFC`, `HFB`, `CRS`, `LPRA`, `AZL`, `CFR`, `Baseline4`, `Baseline5`, and `RandomPlayer`, plus baseline anchors `B1`-`B5`. Tournament settings were `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=200`, `num_workers=4`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, and `timeout_buffer=0.5`. The 15-player pool was padded with 1 `RandomPlayer` to 16 entrants. Result file: `results/tournament/2026-04-10_01-30-49_all_models_vs_public.json`.
- Metrics: Top custom players were `CFR 2.336875 / 11.425 / 1539.25 / 100.00`, `AZL 2.34375 / 11.315 / 1537.15 / 100.00`, `HFB 2.355 / 11.6625 / 1533.95 / 100.00`, and `LPRA 2.356875 / 11.595 / 1531.05 / 100.00`, all with `TO/EXC/DQ/ERR=0/0/0/0`. The trailing rule-based modes were `CFC 2.47 / 12.80 / 1504.73 / 17.70`, `LFS 2.489375 / 12.93125 / 1500.65 / 3.41`, and `CRS 2.5475 / 13.25 / 1492.94 / 0.00`, again with zero failure markers.
- Comparison to baseline: The strongest baseline entry in this shared field was `Baseline5` at `2.3675 / 11.86125 / 1529.51`. `CFR` beat it by `-0.030625 avg_rank`, `-0.43625 avg_score`, and `+9.74 est_elo`; `AZL` by `-0.02375`, `-0.54625`, and `+7.64`; `HFB` by `-0.0125`, `-0.19875`, and `+4.44`; `LPRA` by `-0.010625`, `-0.26625`, and `+1.54`. `CFC`, `LFS`, and `CRS` trailed the best baseline by `+0.1025`, `+0.121875`, and `+0.18 avg_rank` respectively.
- Outcome: Mixed overall, but improved for the top four custom players.
- Representative failures: `CFR` worst partitions were `131` (`avg_rank=3.75`, `avg_score=25.00`), `194` (`3.75`, `24.25`), and `81` (`3.75`, `20.25`). `AZL` worst partitions were `195` (`4.00`, `23.25`), `163` (`3.625`, `26.50`), and `58` (`3.50`, `24.75`). `HFB` worst partitions were `129` (`3.75`, `21.50`), `132` (`3.50`, `20.75`), and `177` (`3.50`, `17.75`). `LPRA` worst partitions were `6` (`4.00`, `24.00`), `153` (`3.75`, `23.50`), and `17` (`3.50`, `23.75`). `CRS` remained the weakest custom model here, with `55` last-place partitions and worst partitions `70` (`3.75`, `21.00`), `148` (`3.75`, `21.00`), and `53` (`3.75`, `20.25`).
- Interpretation: In the full shared field, the strongest rule-based modes (`HFB`, `LPRA`) stay surprisingly close to `CFR` and `AZL`, while the more conservative/reset-oriented modes drop off. This makes `HFB` and `LPRA` strong candidates for opponent pools or RL teacher policies, but `CFR` and `AZL` still lead the custom stack overall.
- Recommendation: Improve. Use `HFB` and `LPRA` as the two rule-based modes worth keeping on the critical path, and if the goal is a teacher/opponent set for RL, prioritize those plus the weaker modes for diversity rather than treating all five as equally competitive main agents.
- Status: Complete.

## 2026-04-10 - Follow-up training direction from current evals

- Target: Post-eval recommendation for the next learning-based iteration, grounded in the current public-baseline tournament results for `CFR`, `AZL`, and the five rule-based modes.
- Setup: Synthesized from the completed 200-partition public-baseline runs logged above, especially the standalone rule-based evaluations and the shared-field tournament in `results/tournament/2026-04-10_01-30-49_all_models_vs_public.json`.
- Metrics: The strongest overall custom players in the shared field were `CFR 2.336875`, `AZL 2.34375`, `HFB 2.355`, and `LPRA 2.356875` by `avg_rank`; the weaker rule-based diversity policies were `CFC 2.47`, `LFS 2.489375`, and `CRS 2.5475`.
- Comparison to baseline: `CFR`, `AZL`, `HFB`, and `LPRA` all finished ahead of the strongest baseline entry (`Baseline5` at `2.3675`) in the shared-field tournament, while `CFC`, `LFS`, and `CRS` did not.
- Outcome: Enough signal exists to define a teacher/opponent pool for learning experiments.
- Representative failures: The weaker rule-based modes still produce many bad partitions in the public-baseline field, especially `CRS` (`55` last-place partitions), so they should not be treated as equally strong teachers even though they remain useful for coverage and opponent diversity.
- Interpretation: The evidence supports a small high-quality teacher set rather than adding many more similar rule-based agents first. `CFR`, `AZL`, `HFB`, and `LPRA` are the best current candidates for supervised data generation, while `CFC`, `LFS`, `CRS`, and `RandomPlayer` are better viewed as diversity opponents.
- Recommendation: Improve. If the next step is a learned model, start with offline imitation/distillation from cross-play logs of `CFR`, `AZL`, `HFB`, and `LPRA`, then consider conservative off-policy fine-tuning afterward instead of jumping straight to plain offline DQN.
- Status: Logged as a planning recommendation; no code changes or new benchmark run in this entry.

## 2026-04-10 - ISMCTS, NFSP, and HumanStrategyPortfolio eval vs random and public baselines

- Target: `ISMCTSPlayer` in `src/players/ISMCTS`, `NFSPPlayer` in `src/players/NFSP`, and `HumanStrategyPortfolioPlayer` in `src/players/rule_human_strategy_portfolio`.
- Setup: `CODEXAGENTS.md` was not present in this checkout; read `README.md`, `docs/current_findings.md`, and `docs/eval_log.md` first. Used `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` (`Python 3.13.11`) because it can import `src.players.TA.public_baselines1`. Existing targeted tests were sanity-checked by direct function invocation under that interpreter: all `14` functions across `tests/test_ismcts_nfsp_players.py` and the relevant `HumanStrategyPortfolioPlayer` coverage in `tests/test_rule_based_players.py` passed. Then ran six `run_tournament.py` evaluations with `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=200`, and `num_workers=4`. Random setup: `candidate + 3 RandomPlayer`s. Stronger setup: `candidate + Baseline4 + Baseline5 + RandomPlayer`, with baseline anchors `B1`-`B5`. Result files: `results/tournament/2026-04-10_16-42-13_eval_ismcts_random.json`, `results/tournament/2026-04-10_16-44-56_eval_nfsp_random.json`, `results/tournament/2026-04-10_16-45-06_eval_human_strategy_portfolio_random.json`, `results/tournament/2026-04-10_16-53-18_eval_ismcts_public.json`, `results/tournament/2026-04-10_16-48-37_eval_nfsp_public.json`, and `results/tournament/2026-04-10_16-45-42_eval_human_strategy_portfolio_public.json`.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, wall-clock `real` time, `TO/EXC/DQ/ERR`, and representative worst partitions.
- Raw results: `ISMCTSPlayer` random=`2.221875 / 9.8475 / 1548.91`, wall-clock `258.71s`, `TO/EXC/DQ/ERR=0/0/0/0`; public=`2.433125 / 11.92 / 1517.02`, `Score=0.00`, wall-clock `272.79s`, `TO/EXC/DQ/ERR=0/0/0/0`. `NFSPPlayer` random=`1.853125 / 7.80375 / 1620.25`, wall-clock `147.19s`, `0/0/0/0`; public=`2.15125 / 9.73625 / 1579.37`, `Score=100.00`, wall-clock `165.71s`, `0/0/0/0`. `HumanStrategyPortfolioPlayer` random=`1.82875 / 7.62875 / 1625.50`, wall-clock `4.50s`, `0/0/0/0`; public=`2.201875 / 10.38 / 1570.30`, `Score=100.00`, wall-clock `25.93s`, `0/0/0/0`.
- Comparison to baseline: `ISMCTSPlayer` beat the best `RandomPlayer` in its random run by `-0.33125 avg_rank`, `-2.975 avg_score`, and `+58.27 est_elo`, but lost to the strongest public-baseline entry by `+0.165625 avg_rank`, `+1.24625 avg_score`, and `-34.98 est_elo`; it also trailed prior logged `HFB` (`2.221875`) and `CFR` / `AZL` public runs (`2.12` / `2.173125`). `NFSPPlayer` beat the best `RandomPlayer` by `-0.83875 avg_rank`, `-5.80375 avg_score`, and `+156.01 est_elo`, and beat the strongest public-baseline entry by `-0.13125 avg_rank`, `-0.90 avg_score`, and `+26.65 est_elo`; relative to prior logged custom public runs, it was slightly worse than `CFR` (`+0.03125 avg_rank`) but slightly better than `AZL` (`-0.021875 avg_rank`). `HumanStrategyPortfolioPlayer` beat the best `RandomPlayer` by `-0.86875 avg_rank`, `-6.035 avg_score`, and `+162.53 est_elo`, and beat the strongest public-baseline entry by `-0.064375 avg_rank`, `-0.165 avg_score`, and `+20.25 est_elo`; relative to prior logged rule-based public runs, it improved on `HFB` by `-0.02 avg_rank` but with `+0.41125 avg_score`, and improved on `LPRA` by `-0.06 avg_rank` and `-0.20 avg_score`.
- Outcome: Mixed overall. `NFSPPlayer` and `HumanStrategyPortfolioPlayer` improved on both the random and public baseline comparisons; `ISMCTSPlayer` improved on the random baseline only and regressed below the public-baseline field.
- Representative failures: `ISMCTSPlayer` random worst partitions were `40` (`avg_rank=3.375`, `avg_score=20.0`), `70` (`3.375`, `18.25`), and `184` (`3.25`, `17.5`); public worst partitions were `68` (`3.875`, `21.5`), `99` (`3.75`, `20.5`), and `112` (`3.75`, `20.25`). `NFSPPlayer` random worst partitions were `140` (`3.5`, `18.25`), `25` (`3.25`, `19.25`), and `197` (`3.125`, `17.5`); public worst partitions were `50` (`3.5`, `16.75`), `101` (`3.5`, `16.75`), and `16` (`3.25`, `18.25`). `HumanStrategyPortfolioPlayer` random worst partitions were `111` (`3.0`, `16.75`), `17` (`2.875`, `14.25`), and `38` (`2.875`, `14.0`); public worst partitions were `12` (`3.25`, `21.0`), `161` (`3.25`, `20.75`), and `129` (`3.25`, `17.75`).
- Interpretation: `NFSPPlayer` currently gives the strongest rank/score line against the public baselines among these three targets, but it is still far slower than the portfolio rule agent. `HumanStrategyPortfolioPlayer` is the best time-efficiency option here: it stayed baseline-beating on both setups and finished the public benchmark about `6.4x` faster than `NFSPPlayer` and about `10.5x` faster than `ISMCTSPlayer`. `ISMCTSPlayer` does not justify its runtime cost on the stronger setup in its current bounded-search form.
- Recommendation: Improve `NFSPPlayer` if the goal is strongest performance among these three; keep `HumanStrategyPortfolioPlayer` as the safest lightweight deployable option; redesign rather than tune-first for `ISMCTSPlayer`, because the current formulation is slower and weaker than the better alternatives on the public-baseline benchmark.
- Status: Complete. No code changes were made.

## 2026-04-10 - Improvement rerun for ISMCTS, NFSP, and HumanStrategyPortfolio

- Target: Post-edit rerun for `ISMCTSPlayer`, `NFSPPlayer`, and `HumanStrategyPortfolioPlayer`.
- Setup: Used `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` (`Python 3.13.11`) and the smallest repeated public-baseline slice that still gives comparative signal. Each benchmark used `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=40`, and `num_workers=4`. The lineup for each standalone run was `candidate + Baseline4 + Baseline5 + RandomPlayer`, with baseline anchors `Baseline1`-`Baseline5`. Pre-change result files were `results/tournament/2026-04-10_18-06-16_pre_ismcts_public_40.json`, `results/tournament/2026-04-10_18-07-13_pre_nfsp_public_40.json`, and `results/tournament/2026-04-10_18-06-35_pre_portfolio_public_40.json`. Final post-change result files were `results/tournament/2026-04-10_18-22-44_pre_ismcts_public_40.json`, `results/tournament/2026-04-10_18-29-06_pre_nfsp_public_40.json`, and `results/tournament/2026-04-10_18-32-18_pre_portfolio_public_40.json`.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, and `TO/EXC/DQ`.
- Raw results:
  - `ISMCTSPlayer`: pre=`2.35 / 11.25625 / 1528.82`, post=`2.240625 / 10.63125 / 1559.01`, `TO/EXC/DQ=0/0/0` both runs.
  - `NFSPPlayer`: pre=`2.21875 / 10.09375 / 1565.93`, post=`2.13125 / 9.8125 / 1582.26`, `TO/EXC/DQ=0/0/0` both runs.
  - `HumanStrategyPortfolioPlayer`: pre=`2.209375 / 10.28125 / 1559.98`, post=`2.15625 / 9.8875 / 1577.09`, `TO/EXC/DQ=0/0/0` both runs.
- Comparison to baseline: On this matched mini-benchmark, all three edited players improved versus their own pre-change versions. Delta summary:
  - `ISMCTSPlayer`: `avg_rank -0.109375`, `avg_score -0.62500`, `est_elo +30.19`
  - `NFSPPlayer`: `avg_rank -0.08750`, `avg_score -0.28125`, `est_elo +16.33`
  - `HumanStrategyPortfolioPlayer`: `avg_rank -0.053125`, `avg_score -0.39375`, `est_elo +17.11`
- Outcome: Improved.
- Representative failures: Three intermediate variants were explicitly discarded before the final rerun:
  - an earlier ungated ISMCTS redesign reached only `2.41875 / 11.175 / 1518.21` in [results/tournament/2026-04-10_18-18-31_pre_ismcts_public_40.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-10_18-18-31_pre_ismcts_public_40.json)
  - a heavier phase-aware NFSP variant reached `2.29375 / 11.05625 / 1552.36` with `timeout_count=133` in [results/tournament/2026-04-10_18-25-28_pre_nfsp_public_40.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-10_18-25-28_pre_nfsp_public_40.json)
  - a broader phase-aware portfolio variant reached `2.3 / 11.825 / 1552.77` in [results/tournament/2026-04-10_18-28-42_pre_portfolio_public_40.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-10_18-28-42_pre_portfolio_public_40.json)
- Interpretation: The winning direction for this pass was selective complexity. ISMCTS improved once search was gated and budgeted more tightly; NFSP improved once the heavier variant was abandoned in favor of cached lightweight mixing plus exact `4`-card endgames; the portfolio improved once the extra phase switching was removed and the design returned to the tighter HFB/LPRA/CRS blend.
- Recommendation: Keep these final versions as the current local defaults for the three targets, and if more benchmarking time is available later, validate the same changes on the larger 200-partition public-baseline setup to separate real gains from 40-partition variance.
- Status: Complete.

## 2026-04-11 - Large shared-field tournament review from `2026-04-10_20-00-44_example_3.json`

- Target: Shared-field review of all current custom players plus public baselines, using the already-produced tournament result `results/tournament/2026-04-10_20-00-44_example_3.json`.
- Setup: Read the result JSON and existing findings only; no code changes and no rerun. The saved config matches `configs/tournament/example_3.json`: `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=2000`, `num_workers=40`. The custom lineup was `LFS`, `CFC`, `HFB`, `CRS`, `LPRA`, `HSP`, `AZL`, `CFR`, `ISMCTS`, `NFSP`, `Baseline4`, `Baseline5`, and `RandomPlayer`, with baseline anchors `B1`-`B5`. Because the pool size was not divisible by 4, the runner padded with `2` extra `RandomPlayer`s.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, calibrated `Score`, and failure markers with emphasis on `timeout_count`.
- Raw results: Custom-player ordering by `avg_rank` was `CFR 2.2754375 / 11.097625 / 1551.94 / TO=9`, `HSP 2.2930625 / 11.207 / 1547.51 / TO=0`, `HFB 2.3069375 / 11.220875 / 1544.96 / TO=0`, `AZL 2.347875 / 11.653375 / 1536.08 / TO=0`, `LPRA 2.3581875 / 11.625375 / 1534.32 / TO=0`, `LFS 2.4303125 / 12.355125 / 1515.68 / TO=0`, `ISMCTS 2.4669375 / 12.520375 / 1507.46 / TO=19`, `NFSP 2.475375 / 12.69775 / 1505.35 / TO=10730`, `CRS 2.4760625 / 12.64925 / 1505.80 / TO=0`, and `CFC 2.48675 / 12.78725 / 1502.96 / TO=0`. The strongest baseline entry was `B5` at `2.3831875 / 11.71575 / 1527.31`. Result file only; no new wall-clock measurement was produced in this review entry.
- Comparison to baseline: `CFR`, `HSP`, `HFB`, `AZL`, and `LPRA` all finished ahead of the strongest baseline entry by `avg_rank`; their deltas vs `B5` were `-0.10775`, `-0.090125`, `-0.07625`, `-0.0353125`, and `-0.025` respectively. `LFS`, `ISMCTS`, `NFSP`, `CRS`, and `CFC` all finished behind `B5` by `+0.047125`, `+0.08375`, `+0.0921875`, `+0.092875`, and `+0.1035625` respectively. Relative to the earlier 200-partition all-custom shared-field run, this larger 2000-partition result still supports the same top family, but now adds `HSP` as a clear top-tier custom entrant and gives a much stronger sample against variance.
- Outcome: Improved confidence in the top custom stack; concerning regression/risk signal for `NFSPPlayer` under this large shared-field setup because of heavy timeout counts.
- Representative failures: `CFR` worst partitions were `723` (`avg_rank=4.0`, `avg_score=25.0`), `794` (`4.0`, `19.0`), and `541` (`3.75`, `22.0`). `HSP` worst partitions were `126` (`4.0`, `29.25`), `1823` (`4.0`, `27.25`), and `510` (`4.0`, `25.0`). `ISMCTS` worst partitions were `294` (`4.0`, `27.75`), `271` (`4.0`, `25.25`), and `440` (`4.0`, `24.5`), plus `timeout_count=19`. `NFSP` worst partitions were `348` (`4.0`, `31.75`), `423` (`4.0`, `27.5`), and `993` (`4.0`, `27.0`), and it recorded `10730` timeouts over `80000` turns in this tournament.
- Interpretation: This result is strong enough to treat the top manual agents as a stable teacher/opponent pool. `CFR`, `HSP`, `HFB`, `AZL`, and `LPRA` all cleared the strongest baseline over a much larger sample, while `NFSP` and `ISMCTS` did not hold up in the same field. The most important new fact is not just rank ordering; it is that `NFSPPlayer` is currently not robust enough for large-scale training-data generation or deployment in this configuration because of the very large timeout count.
- Recommendation: Start the learning track only as a small offline imitation/distillation project grounded in logs from `CFR`, `HSP`, `HFB`, `AZL`, and `LPRA`. Do not start with heavy online RL or use `NFSPPlayer` / `ISMCTSPlayer` as core teachers yet. The next practical step is to build a dataset pipeline from cross-play among the top five confirmed models, then train and validate a lightweight policy/value learner against this same shared-field benchmark.
- Status: Complete review of an existing tournament result; no code changes and no rerun.

## 2026-04-11 - CrossPlayImitationPlayer mini public-baseline eval

- Target: `CrossPlayImitationPlayer` from `src.players.crossplay_imitation`, using the model the user trained locally from `results/training/crossplay_examples.jsonl`.
- Setup: User-run tournament in the `FAI_final` conda env with `python run_tournament.py --config configs/tournament/example.json --player-cfg /tmp/crossplay_eval_players.json --tournament-cfg /tmp/crossplay_eval_tournament.json`. The player lineup was `CrossPlayImitationPlayer`, `Baseline4`, `Baseline5`, and `RandomPlayer`. The tournament override used `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=8`, and `num_workers=1`. Engine settings came from `configs/tournament/example.json`: `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`.
- Metrics: `avg_rank`, `est_elo`, `avg_score`, `games`.
- Raw results: `CrossPlayImitationPlayer` finished `1st` with `avg_rank=2.02`, `est_elo=1591`, `avg_score=9.25`, `games=32`. Opponents were `Baseline5 2.33 / 1532 / 10.69 / 32`, `Baseline4 2.47 / 1507 / 13.56 / 32`, and `RandomPlayer 3.19 / 1370 / 18.75 / 32`.
- Comparison to baseline: On this mini benchmark, `CrossPlayImitationPlayer` beat `Baseline5` by `-0.31 avg_rank`, `-1.44 avg_score`, and `+59 est_elo`; it beat `Baseline4` by `-0.45 avg_rank`, `-4.31 avg_score`, and `+84 est_elo`.
- Outcome: Promising on a small sample.
- Representative failures: No TO/EXC/DQ markers were reported in the saved standings output. The main limitation is sample size: this run covered only `32` games for the learned player.
- Interpretation: The first offline-trained draft is strong enough to beat both `Baseline4` and `Baseline5` on this 8-partition smoke tournament, which is encouraging evidence that the cross-play learning path is viable. It is still too small a run to treat as stable tournament strength.
- Recommendation: Improve. Increase dataset size, mix in lower-weight diversity labels, train for many more epochs, then rerun the same matchup at a larger partition count before making broader performance claims.
- Status: Complete. This entry records the user-run benchmark result and motivates the next training iteration.

## 2026-04-12 - Larger mixed-dataset CrossPlayImitationPlayer follow-up eval

- Target: `CrossPlayImitationPlayer` after retraining on a much larger mixed-label dataset generated from `CFR,HSP,HFB,AZL,LPRA,LFS,CFC,CRS,IMIT,RND`.
- Setup: User-run data generation used `python scripts/generate_crossplay_data.py --output results/training/crossplay_examples_diverse.jsonl --teacher-labels CFR,HSP,HFB,AZL,LPRA,LFS,CFC,CRS,IMIT,RND --teacher-weight LFS=0.35 --teacher-weight CFC=0.35 --teacher-weight CRS=0.35 --teacher-weight IMIT=0.35 --teacher-weight RND=0.20 --games-per-lineup 40 --include-rotations --max-lineups 80 --lineup-seed 11 --base-seed 11 --n-rounds 10`, which produced `115200` logged decisions with total example weight `76230.00`. User-run training used `python scripts/train_crossplay_model.py --dataset results/training/crossplay_examples_diverse.jsonl --output src/players/crossplay_imitation/crossplay_model.json --epochs 5000 --learning-rate 0.01 --l2 0.0001`, which reported train `accuracy=0.6996` and `loss=0.8884`.
- Mini-eval setup: User then ran `python run_tournament.py --config configs/tournament/example.json --player-cfg /tmp/crossplay_eval_players.json --tournament-cfg /tmp/crossplay_eval_tournament.json` with lineup `CrossPlayImitationPlayer`, `Baseline4`, `Baseline5`, and `RandomPlayer`; engine `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`; tournament `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=40`, `num_workers=4`. Result file: [results/tournament/2026-04-12_10-24-23_example.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-12_10-24-23_example.json).
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `games`, and failure markers.
- Raw results: `CrossPlayImitationPlayer` finished `1st` with `avg_rank=2.153125`, `avg_score=10.09375`, `est_elo=1564.52`, `games=160`, `DQ/TO/EXC=0/0/0`. Opponents were `B4 2.2625 / 11.075 / 1544.31`, `B5 2.41875 / 11.7375 / 1515.82`, and `RND 3.165625 / 19.08125 / 1375.35`.
- Comparison to baseline: `CrossPlayImitationPlayer` beat `B4` by `-0.109375 avg_rank`, `-0.98125 avg_score`, and `+20.21 est_elo`; it beat `B5` by `-0.265625 avg_rank`, `-1.64375 avg_score`, and `+48.71 est_elo`. Relative to the earlier 8-partition `XPI` mini eval, the larger sample regressed from `2.02 / 9.25 / 1591` to `2.153125 / 10.09375 / 1564.52`, which is consistent with the earlier 8-partition result being optimistic noise.
- Outcome: Improved confidence that `CrossPlayImitationPlayer` is baseline-beating on this narrow public-mini field.
- Representative failures: No `DQ`, `TO`, or `EXC` markers were recorded in the mini-eval standings.
- Interpretation: The larger mixed dataset successfully preserved the narrow-field baseline-beating behavior on a substantially larger sample. On this matched mini benchmark, `XPI` is essentially tied with the previously logged `HumanStrategyPortfolioPlayer` (`2.15625`) and still trails the previously logged `NFSPPlayer` (`2.13125`) by a small margin.
- Recommendation: Evaluate on the broader mixed field before treating this as a top-tier result, because the mini benchmark only tests `XPI` against `B4`, `B5`, and `RandomPlayer`.
- Status: Complete.

## 2026-04-12 - Shared-field review for `2026-04-12_11-16-39_example_3.json`

- Target: Broad shared-field review after adding `CrossPlayImitationPlayer` to `configs/tournament/example_3.json`.
- Setup: User ran `python run_tournament.py --config configs/tournament/example_3.json` in the `FAI_final` environment. Saved config: `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=2000`, `num_workers=40`. The custom pool was `LFS`, `CFC`, `HFB`, `CRS`, `LPRA`, `HSP`, `AZL`, `CFR`, `ISMCTS`, `NFSP`, `XPI`, `Baseline4`, `Baseline5`, and `RandomPlayer`, with baseline anchors `B1`-`B5`. Because the total player count was not divisible by 4, the runner padded with one extra `RandomPlayer`. Result file: [results/tournament/2026-04-12_11-16-39_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-12_11-16-39_example_3.json).
- Metrics: `avg_rank`, `avg_score`, `est_elo`, calibrated `Score`, `DQ`, and `TO`.
- Raw results: The top eight labeled entrants by `avg_rank` were `CFR 2.3186875 / 11.451625 / 1541.65 / DQ=1 / TO=42`, `HFB 2.33 / 11.49625 / 1539.31`, `HSP 2.345 / 11.64975 / 1536.58`, `AZL 2.3610625 / 11.7805 / 1531.56`, `LPRA 2.40025 / 12.075125 / 1524.52`, `B4 2.407125 / 12.181875 / 1521.54`, `B3 2.4123125 / 12.0005 / 1520.21`, and `XPI 2.414375 / 12.20525 / 1519.58 / DQ=0 / TO=0`. `B5` followed at `2.414625 / 12.112375 / 1518.74`.
- Comparison to baseline: In this shared field, `XPI` beat `B5` by `-0.00025 avg_rank` and `+0.85 est_elo`, but trailed `B4` by `+0.00725 avg_rank` and `-1.96 est_elo`, and trailed `B3` by `+0.0020625 avg_rank` and `-0.62 est_elo`. It also trailed every top custom entrant `CFR`, `HFB`, `HSP`, `AZL`, and `LPRA`.
- Comparison to prior shared-field run: The previous shared-field result [results/tournament/2026-04-10_20-00-44_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-10_20-00-44_example_3.json) did not include `XPI`, so direct before/after deltas for `XPI` are not available. The top custom pack remained the same five labels, although their exact values shifted because the field composition changed when `XPI` was added.
- Outcome: Mixed. `XPI` is competitive enough to sit near the upper baseline tier with zero failure markers, but it is not yet a top shared-field custom agent.
- Representative failures: `XPI` itself recorded `DQ=0`, `TO=0`, `EXC=0`. The broader field still showed robustness problems for other agents, especially `NFSP` (`DQ=2`, `TO=13158`) and `ISMCTS` (`TO=118`).
- Interpretation: The larger mixed-label training pass improved narrow-field confidence but did not carry `XPI` into the top shared-field pack. `XPI` landed in an upper-middle cluster with `B4`, `B3`, `B5`, and the duplicated public-baseline entries, rather than with `CFR/HFB/HSP/AZL/LPRA`. Combined with the still-near-`0.70` train accuracy after `115200` decisions and `5000` epochs, the evidence points more toward a target-quality / feature-capacity bottleneck than an optimization-steps bottleneck.
- Recommendation: Improve, but do not just add more epochs. The best next experiment is a size-matched control trained only on the strong top-five teachers (`CFR,HSP,HFB,AZL,LPRA`) while keeping the evaluation protocol unchanged; this will directly test whether weaker target labels are diluting the student policy.
- Status: Complete review of the user-run shared-field result.

## 2026-04-12 - Top-five-only control mini eval

- Target: `CrossPlayImitationPlayer` loaded from `src/players/crossplay_imitation/crossplay_model_top5.json`, trained only on the strong teacher pool `CFR,HSP,HFB,AZL,LPRA`.
- Setup: User-run data generation used `python scripts/generate_crossplay_data.py --output results/training/crossplay_examples_top5_115200.jsonl --teacher-labels CFR,HSP,HFB,AZL,LPRA --games-per-lineup 144 --include-rotations --base-seed 11 --n-rounds 10`, which produced `103680` logged decisions. User-run training used `python scripts/train_crossplay_model.py --dataset results/training/crossplay_examples_top5_115200.jsonl --output src/players/crossplay_imitation/crossplay_model_top5.json --epochs 5000 --learning-rate 0.01 --l2 0.0001`, which reported train `accuracy=0.6994` and `loss=0.8542`. Evaluation used the same mini field as the earlier matched test: lineup `XPI_TOP5`, `Baseline4`, `Baseline5`, and `RandomPlayer`; engine `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`; tournament `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=40`, `num_workers=4`. Result file: [results/tournament/2026-04-12_18-55-23_example.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-12_18-55-23_example.json).
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `games`, and failure markers.
- Raw results: `B4` finished `1st` at `2.328125 / 11.6375 / 1530.72`; `XPI_TOP5` finished `2nd` at `2.340625 / 11.7125 / 1528.50`; `B5` finished `3rd` at `2.39375 / 12.175 / 1519.07`; `RND` finished `4th` at `2.9375 / 17.44375 / 1421.71`. `XPI_TOP5` recorded `DQ/TO/EXC=0/0/0`.
- Comparison to baseline: `XPI_TOP5` trailed `B4` by `+0.0125 avg_rank`, `+0.075 avg_score`, and `-2.22 est_elo`, but still beat `B5` by `-0.053125 avg_rank`, `-0.4625 avg_score`, and `+9.42 est_elo`.
- Comparison to the larger mixed-label model on the same mini setup: The earlier mixed-label model produced `2.153125 / 10.09375 / 1564.52` on [results/tournament/2026-04-12_10-24-23_example.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-12_10-24-23_example.json). Relative to that matched result, `XPI_TOP5` regressed by `+0.1875 avg_rank`, `+1.61875 avg_score`, and `-36.02 est_elo`.
- Outcome: Regressed.
- Representative failures: No `DQ`, `TO`, or `EXC` markers occurred. The failure is performance, not robustness.
- Interpretation: The top-five-only control does not support the earlier “weaker labels are diluting the student” hypothesis. Removing the diversity labels made the mini-field result worse, while train accuracy stayed essentially unchanged (`0.6996 -> 0.6994`), reinforcing the view that the main bottleneck is model expressiveness rather than simple label filtering or additional epochs.
- Recommendation: Do not spend more time on top-five-only retrains under the current linear student design. Keep the larger mixed-label dataset and raise student capacity next, starting with a safe phase-specific scorer before trying a tiny MLP.
- Status: Complete.

## 2026-04-13 - FullCFRPlayer action-latency probe after timeout-budget cap

- Target: `FullCFRPlayer` in [src/players/cfr/full_cfr_player.py](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/src/players/cfr/full_cfr_player.py).
- Setup: Ran a focused local latency probe under `/home/thoamslai/miniconda3/envs/FAI_final/bin/python` on `2` seeded `Engine({"n_players": 4, "n_rounds": 10})` games with lineup `FullCFRPlayer + 3 RandomPlayer`s. For each round, the benchmark timed `player.action(hand, history)` before advancing the actual engine state. The pre-change numbers came from the same probe executed immediately before the edit; the post-change numbers came from the same probe executed immediately after the edit.
- Metrics: Mean and max `action()` wall-clock time by current `hand_size`, plus the resolved `(sample_budget, iterations)` search budget after the edit.
- Raw results:
  - `hand_size=10`: budget `(1, 5)`, pre `739.35 / 755.17 ms`, post `279.71 / 302.04 ms`
  - `9`: budget `(1, 7)`, pre `433.31 / 473.62 ms`, post `210.50 / 224.44 ms`
  - `8`: budget `(1, 12)`, pre `535.06 / 774.81 ms`, post `182.76 / 221.75 ms`
  - `7`: budget `(1, 14)`, pre `429.62 / 463.91 ms`, post `95.84 / 107.31 ms`
  - `6`: budget `(2, 16)`, pre `219.47 / 249.54 ms`, post `130.65 / 149.93 ms`
  - `5`: budget `(4, 19)`, pre `118.15 / 134.00 ms`, post `165.90 / 218.00 ms`
- Comparison to baseline: The fix materially reduced the early and mid-game heavy cases that were closest to the engine timeout. Mean-time deltas were `-459.64 ms` (`10` cards), `-222.81 ms` (`9`), `-352.30 ms` (`8`), `-333.78 ms` (`7`), and `-88.82 ms` (`6`). The `5`-card stage regressed by `+47.75 ms`, but it remained far from the `1.0s` timeout budget in this probe.
- Outcome: Improved for the timeout target.
- Representative failures: This was a timing probe only; it did not measure tournament `avg_rank` or `TO` counts directly.
- Interpretation: The new work cap is doing the intended job. It sharply lowers the CFR player’s worst local action times in the large-hand rounds that dominate timeout risk, while leaving the late-game regime essentially untouched.
- Recommendation: Run a later shared-field rerun in the user’s `example_3.json` setup to verify whether the lower per-turn latency translates into lower `TO` counts without materially hurting CFR’s standings.
- Status: Complete microbenchmark.

## 2026-04-13 - FullCFRPlayer matched mini shared-field rerun after late-phase-only gate

- Target: `FullCFRPlayer` in [src/players/cfr/full_cfr_player.py](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/src/players/cfr/full_cfr_player.py), after limiting CFR search to hand sizes `6` and below.
- Setup: Used the same broad `example_3` field as the user’s shared-field run, but with a smaller matched slice to compare pre/post behavior under the same concurrency: `python run_tournament.py --config configs/tournament/example_3.json --tournament-cfg /tmp/cfr_timeout_probe_tournament.json`, where the override was `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=100`, and `num_workers=40`. Pre-change result file: [results/tournament/2026-04-13_13-46-21_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-13_13-46-21_example_3.json). Post-change result file: [results/tournament/2026-04-13_13-49-45_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-13_13-49-45_example_3.json).
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `timeout_count`, and `dq_count` for `CFR`, with the same field size and worker count before and after the edit.
- Raw results:
  - Pre-change `CFR`: `avg_rank=2.21875`, `avg_score=10.7175`, `est_elo=1564.78`, `TO=2`, `DQ=0`
  - Post-change `CFR`: `avg_rank=2.2775`, `avg_score=11.02`, `est_elo=1549.05`, `TO=0`, `DQ=0`
- Comparison to baseline: On this matched mini shared-field slice, the late-phase-only gate removed CFR’s observed timeouts (`2 -> 0`) but regressed performance slightly: `avg_rank +0.05875`, `avg_score +0.3025`, and `est_elo -15.73`. `CFR` still finished `1st` in the post-change mini run.
- Additional field context: Other timeout-heavy players were still unstable on the same matched slice, with `ISMCTS TO=5 -> 3` and `NFSP TO=721 -> 673`, so the run still reflects the intended high-contention environment rather than a no-timeout toy setup.
- Outcome: Improved reliability, with mild performance regression.
- Representative failures: No `DQ` or `TO` remained for `CFR` in the post-change run, but its `avg_rank`/`avg_score` line worsened slightly relative to the pre-change mini slice.
- Interpretation: The early-round CFR gate solved the immediate timeout problem on this matched high-worker mini benchmark. The tradeoff is real but limited: CFR stayed `1st`, yet lost some edge on rank/score.
- Recommendation: If the priority is robust tournament execution under the shared-field `40`-worker setup, keep this version. If later runs show the strength regression matters more than the timeout reduction, the next tuning axis should be a softer threshold or a cheaper early-phase search surrogate rather than re-enabling full early-round CFR outright.
- Status: Complete matched mini rerun.

## 2026-04-13 - User-run full shared-field rerun after late-phase-only gate

- Target: `FullCFRPlayer` on the full broad `example_3` shared-field setup, using the kept late-phase-only gate version.
- Setup: User ran `python run_tournament.py --config configs/tournament/example_3.json` in the `FAI_final` environment. Result file: [results/tournament/2026-04-13_14-48-04_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-13_14-48-04_example_3.json). This is the same broad field shape as the earlier user-run `2026-04-13_13-37-09_example_3.json` result: `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=2000`, and `num_workers=40`.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `timeout_count`, and `dq_count`.
- Raw results: `CFR` finished `1st` with `avg_rank=2.306`, `avg_score=11.35325`, `est_elo=1543.80`, `TO=15`, `DQ=0`.
- Comparison to baseline: Relative to the earlier user-run broad-field result [results/tournament/2026-04-13_13-37-09_example_3.json](/home/thoamslai/projects/myproject/2026-04-13_13-37-09_example_3.json), `CFR` improved on both robustness and standings: `avg_rank 2.3125625 -> 2.306`, `avg_score 11.39775 -> 11.35325`, `est_elo 1543.46 -> 1543.80`, `TO 82 -> 15`, and `DQ 1 -> 0`.
- Outcome: Improved on both performance and timeout robustness.
- Representative failures: The timeout problem was not fully eliminated; `TO=15` remains materially above zero on the full `8000`-game broad-field run.
- Interpretation: This user-run result confirms that the late-phase-only gate is a real improvement rather than just a mini-benchmark artifact. It reduced `CFR` timeouts sharply while slightly improving overall tournament performance.
- Recommendation: Keep the late-phase-only gate as the current `CFR` default. Treat future CFR work as a tuning problem around this version, not a reason to re-enable broad early-round CFR by default.
- Status: Complete. Records the user-run full-field result.

## 2026-04-13 - Selective late-phase CFR variants on matched mini shared-field

- Target: Two experimental `FullCFRPlayer` variants that tried to improve further on the kept late-phase-only gate:
  - risk-gated + top-`k` late-phase CFR
  - top-`k`-only late-phase CFR
- Setup: Both were evaluated on the same matched broad-field mini setup used earlier: `python run_tournament.py --config configs/tournament/example_3.json --tournament-cfg /tmp/cfr_timeout_probe_tournament.json`, where the override was `random_partition`, `duplication_mode="cycle"`, `num_games_per_player=100`, and `num_workers=40`. Comparison baseline: kept late-phase-only result [results/tournament/2026-04-13_13-49-45_example_3.json](/home/thoamslai/projects/myproject/2026-04-13_13-49-45_example_3.json).
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `timeout_count`, and `dq_count` for `CFR`.
- Raw results:
  - Kept late-phase-only baseline: `2.2775 / 11.02 / 1549.05 / TO=0 / DQ=0`
  - Risk-gated + top-`k`: [results/tournament/2026-04-13_15-02-03_example_3.json](/home/thoamslai/projects/myproject/2026-04-13_15-02-03_example_3.json) -> `2.34 / 11.50 / 1535 / TO=0 / DQ=0`
  - Top-`k` only: [results/tournament/2026-04-13_15-06-24_example_3.json](/home/thoamslai/projects/myproject/2026-04-13_15-06-24_example_3.json) -> `2.35 / 11.82 / 1535 / TO=3 / DQ=0`
- Comparison to baseline: Both selective variants regressed versus the kept late-phase-only baseline. The risk-gated + top-`k` variant worsened `avg_rank` by `+0.0625` with no timeout benefit beyond staying at zero. The top-`k`-only variant worsened `avg_rank` by `+0.0725` and also reintroduced timeouts (`0 -> 3`).
- Outcome: Regressed.
- Representative failures: The main failure mode was tournament strength loss. The top-`k`-only variant also lost robustness relative to the kept version.
- Interpretation: In this CFR design, pruning our own late-phase action set to the top heuristic candidates appears to remove too much useful search flexibility. The broader late-phase CFR solve with the existing budgets works better than these more selective variants on the matched mini field.
- Recommendation: Do not keep either selective variant as the default `CFR` implementation. If future work revisits selective CFR, it should probably use softer candidate expansion or a cheaper early-phase surrogate rather than hard top-`k` pruning.
- Status: Complete. Both variants were rejected and rolled back.

## 2026-04-13 - User-run full shared-field follow-up after the kept late-phase-only gate

- Target: `FullCFRPlayer` on the full broad `example_3` shared-field setup, using the kept late-phase-only gate version.
- Setup: User ran `python run_tournament.py --config configs/tournament/example_3.json` in the `FAI_final` environment. Result file: [results/tournament/2026-04-13_15-53-17_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-13_15-53-17_example_3.json). This is the same broad field shape as the earlier same-day user runs: `random_partition`, `duplication_mode="cycle"`, `n_players=4`, `n_rounds=10`, `timeout=1.0`, `timeout_buffer=0.5`, `num_games_per_player=2000`, and `num_workers=40`.
- Metrics: `avg_rank`, `avg_score`, `est_elo`, `timeout_count`, and `dq_count`.
- Raw results: `CFR` finished `1st` with `avg_rank=2.278875`, `avg_score=11.210875`, `est_elo=1550.80`, `TO=6`, `DQ=1`.
- Comparison to baseline: Relative to the earlier user-run broad-field result [results/tournament/2026-04-13_13-37-09_example_3.json](/home/thoamslai/projects/myproject/2026-FAI-Final-Release/results/tournament/2026-04-13_13-37-09_example_3.json), `CFR` improved on rank, score, Elo, and timeout count: `avg_rank 2.3125625 -> 2.278875`, `avg_score 11.39775 -> 11.210875`, `est_elo 1543.46 -> 1550.80`, and `TO 82 -> 6`, while `DQ` stayed at `1`. Relative to the earlier kept-version full-field rerun [results/tournament/2026-04-13_14-48-04_example_3.json](/home/thoamslai/projects/myproject/2026-04-13_14-48-04_example_3.json), it further improved `avg_rank 2.306 -> 2.278875`, `avg_score 11.35325 -> 11.210875`, `est_elo 1543.80 -> 1550.80`, and `TO 15 -> 6`, but regressed `DQ 0 -> 1`.
- Outcome: Improved overall, with one remaining disqualification.
- Representative failures: The timeout problem is much smaller now than in the earlier same-day broad-field reruns, but it is not fully eliminated and one `DQ` still occurred.
- Interpretation: This is the strongest same-day broad-field `CFR` result recorded so far by `avg_rank`, `avg_score`, `est_elo`, and timeout count. The late-phase-only gate appears to be the right default direction for this player in the shared-field tournament. The remaining reliability issue is no longer broad timeout fragility; it is a low but nonzero residual failure tail.
- Recommendation: Keep the current late-phase-only `CFR` as the repo default. If more CFR work is attempted later, optimize for eliminating the last few `TO`/`DQ` markers without undoing the now-confirmed broad-field strength gain.
- Status: Complete. Records the user-run full-field follow-up result.
